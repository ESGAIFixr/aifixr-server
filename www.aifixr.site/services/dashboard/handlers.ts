/**
 * 대시보드 페이지 핸들러
 * 
 * 대시보드 관련 비즈니스 로직 처리
 * - 향후 대시보드 액션 추가 예정
 */

/**
 * 대시보드 핸들러 생성
 * (현재는 빈 핸들러, 추후 확장 예정)
 */
export function createDashboardHandlers() {
  // 향후 대시보드 관련 핸들러 추가
  // 예: handleExportData, handleRefreshStats, handleNavigateToReport 등

  return {
    // 빈 객체 반환 (추후 확장)
  };
}
